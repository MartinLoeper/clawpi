# clawpi-tools

## Version Bumps

Every time you bump the version in `openclaw.plugin.json` and `package.nix`, also:

- Add a new entry to `CHANGELOG.md` describing what changed. Keep entries concise — one line per change.
- Update `docs/tools.md` — add/update the summary table row and the detailed section for any new or changed tools.
